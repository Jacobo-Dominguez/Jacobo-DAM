<Window x:Class="Binding.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Binding"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <DockPanel>
        <StackPanel Name="miPanel" ButtonBase.Click="miPanel_Click" DockPanel.Dock="Top">
            <Button Content="Click Me" Width="100" Height="30" Margin="10"/>
            <Button Content="Click me now" Width="100" Height="30" Margin="10" Click="Button_Click"/>
            <Button Content="Click me please" Width="100" Height="30" Margin="10"/>
        </StackPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="10">
                <TextBlock Text="Enter your name:" Margin="0,0,0,5"/>
                <TextBox Name="nameTextBox" Width="180" Margin="0,0,0,10"/>

                <TextBlock Text="Enter your lastname:" Margin="0,10,0,5"/>
                <TextBox Name="lastnameTextBox" Width="180" Margin="0,0,0,10"/>
            </StackPanel>

            <Canvas Grid.Column="1" Margin="20">
                <TextBlock FontSize="10" FontWeight="Bold"
                        Text="{Binding ElementName=txtNombreOrigen,
                                    Path=Text,
                                    Mode=OneWay}"
                        Canvas.Top="20" 
                        Canvas.Left="0"/>

                <TextBlock FontSize="10" FontWeight="Bold"
                        Text="{Binding ElementName=txtDescriptionOrigen,
                                    Path=Text,
                                    Mode=OneWay}"
                        Canvas.Top="50" 
                        Canvas.Left="0" 
                        TextWrapping="Wrap" 
                        Width="400"/>
            </Canvas>
            
            
        </Grid>
    </DockPanel>
</Window>